{
  "title": "Room",
  "type": "object",
  "properties": {
    "id": {
      "$ref": "../scalars/roomID.json#"
    },
    "ownerID": {
      "$ref": "../scalars/userID.json#"
    },
    "owner": {
      "$ref": "user.json#"
    },
    "sessions": {
      "type": "array",
      "items": {
        "$ref": "session.json#"
      }
    },
    "createdAt": {
      "type": "string",
      "format": "date"
    }
  },
  "required": ["id", "ownerID", "owner", "sessions", "createdAt"],
  "additionalProperties": false
}