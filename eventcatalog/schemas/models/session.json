{
  "title": "Session",
  "type": "object",
  "properties": {
    "id": {
      "$ref": "../scalars/SessionID.json#"
    },
    "roomID": {
      "$ref": "../scalars/roomID.json#"
    },
    "game": {
      "$ref": "../scalars/game.json#"
    },
    "players": {
      "type": "array",
      "items": {
        "$ref": "sessionPlayer.json#"
      }
    },
    "createdAt": {
      "type": "string",
      "format": "date"
    },
    "startedAt": {
      "type": "string",
      "format": "date-time"
    },
    "finishedAt": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["id", "game", "players", "createdAt"],
  "additionalProperties": false
}