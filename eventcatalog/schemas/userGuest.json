{
  "title": "UserGuest",
  "type": "object",
  "goImplements": "User",
  "properties": {
    "id": {
      "$ref": "userID.json#"
    },
    "username": {
      "$ref": "username.json#"
    },
    "role": {
      "$ref": "userRole.json#"
    },
    "createdAt": {
      "type": "string",
      "format": "date"
    },
    "bannedAt": {
      "type": "string",
      "format": "date"
    }
  },
  "required": ["id", "username", "role", "createdAt"],
  "additionalProperties": false
}