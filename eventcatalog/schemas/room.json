{
  "title": "Room",
  "type": "object",
  "properties": {
    "id": {
      "$ref": "roomID.json#"
    },
    "ownerID": {
      "$ref": "userID.json#"
    },
    "owner": {
      "$ref": "user.json#"
    },
    "sessions": {
      "type": "array",
      "items": {
        "$ref": "session.json#"
      }
    },
    "createdAt": {
      "type": "string",
      "format": "date"
    }
  },
  "required": ["id", "ownerID", "owner", "sessions", "createdAt"],
  "additionalProperties": false
}